import { assetPath } from "../../Utils/assets";
import {CONSTANTS} from "./constants.js";

export const assetPath8 = (p) => {
    return assetPath("8/" + p);
};

export const assetPath8Videos = (p) => {
    return assetPath8("videos/" + p);
};

